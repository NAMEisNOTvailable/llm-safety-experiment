name: Export LLaMA2 Chinese 1500 annotation batch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Verify source files
        run: echo ok
