# GLM4 Chinese annotation completion

The 1,500 responses in `data/results/glm4_results_Chinese.jsonl` were exported and annotated externally.

Delivered annotation files contain only:

- `order`
- `idx`
- `label`
- `reason`

The original repository labels were not changed. This temporary export branch is not intended to be merged.